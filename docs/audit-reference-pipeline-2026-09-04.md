Аудит OMNI REELS ANTON, 4 сентября 2026 года.

Это снимок до изменений. Выполненная реализация и её ограничения описаны в [отчёте об улучшениях](implemented-reference-pipeline-2026-09-04.md).

Цель, уточнённая пользователем: разговорный аватар, монтаж с тематическими B-roll и отдельными товарными B-roll без прямого взаимодействия аватара с товаром. Из выбранного референса переносим визуальный сеттинг и сценарную механику, адаптируя несовместимые действия под этот формат. Правильная физика объектов, корректные промпты раскадровки и Omni-видео — обязательные требования.

Ограничение бюджета: платную проверку готового видео сейчас не добавляем. Основная работа — снижение риска артефактов до отправки в Google Omni: согласованный план, существующая проверка раскадровки и локальная валидация финального промпта. Особый приоритет — озвучка. По наблюдениям пользователя четыре слова на блок работают лучше прежних трёх–пяти, но иногда оставляют паузы, которые модель заполняет; при трёх–пяти словах часть речи зажёвывается. Это наблюдения пользователя, отдельный сравнительный прогон в аудите не проводился.

Первоначальный вывод о том, что замена взаимодействия с продуктом на предметный B-roll сама по себе является ошибкой, снят после уточнения пользователя. Это требуемое поведение. Удалять запрет взаимодействия и физические проверки нельзя. Проверять нужно согласованность этой замены во всех планах, промптах и проверках раскадровки.

Главный вывод: проект уже содержит необходимые части, но несколько действующих правил и параллельных цепочек меняют исходную задачу. Удаление неиспользуемых файлов уменьшит объём сопровождения; для улучшения роликов сначала нужно устранить эти подмены.

Проверена локальная рабочая копия: исходники, импорты, точки входа, путь запроса до провайдера и четыре тестовых скрипта. В Git учтены 543 файла; 512 файлов Python/JS/TS содержат 86 431 строку, включая тесты. В `ui/src/lib/server/omni` — 190 файлов и 29 858 строк. Двадцать исходных файлов превышают 500 строк. Эти числа описывают размер, сами по себе они не доказывают избыточность.

Продакшен, фактические очереди и расходы провайдеров не проверялись. Платные генерации не запускались. Код приложения и пользовательские изменения не редактировались.

Приоритетные находки в действующем процессе:

1. **P1. Визуальная проверка не блокирует часть ошибок физики и запрещённого контакта с товаром. Подтверждено локальной проверкой обработки ответов.**

   Текстовый `validatePhysicalScene` проверяет опору продукта, взаимодействие с руками, смену состояния и другие ограничения. Это нужная часть системы. Но общий обработчик визуального QA относит `teleportation`, `physical_action`, `motion` и ряд других кодов к метаданным. Блокирующий список существенно уже физического контракта. Промпт визуального проверяющего тоже ограничивает набор ошибок в основном идентичностью, формой/наличием товара и грубой порчей изображения.

   В локальном воспроизведении искусственный ответ визуального проверяющего со статусом `block`, уверенностью 0,99 и ошибкой `OBJECT_TELEPORTATION` превратился в `pass` с удалённой ошибкой. `PRODUCT_SUPPORT_MISSING` и `PRODUCT_BROLL_HAS_HUMAN_INTERACTION` превратились в предупреждения со статусом `pass`. Это проверка обработки ответа, а не утверждение, что модель обнаружила такие дефекты на реальном видео.

   Доказательства: [physical-scene-validator.ts:79](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/physical-scene-validator.ts:79>), [storyboard-qa-contract.ts:9](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard-qa-contract.ts:9>), [storyboard-vision-contract.ts:22](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard-vision-contract.ts:22>), [storyboard-vision-validator.ts:173](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard-vision-validator.ts:173>).

   Что изменить: согласовать существующий промпт проверяющего и обработчик его ответа с обязательными физическими правилами без добавления нового платного прохода. На раскадровке блокировать подтверждённую потерю опоры, деформацию, неверную форму/масштаб и запрещённый контакт человека с товаром. Динамику по отдельным панелям надёжно проверить нельзя; её риски заранее снижать простыми движениями камеры, устойчивой опорой и неизменными свойствами продукта. Монтажный переход к другому кадру не считать телепортацией объекта внутри непрерывного плана.

2. **P1. Кнопка ручного запуска в настройках ведёт в другой конвейер; его клиент отбрасывает аватар и продукт. Подтверждено исходниками.**

   `Settings → /api/automation/final-videos/manual-run → final_video_jobs → final_video_automation.py` запускает старую генерацию сценария через `mode="mix"` и Python Smart Layer. Это отдельный путь от `omni_automation_jobs` и текущего построения ролика по режиссёрскому анализу.

   Старый оркестратор передаёт `character_id`, `audio_id` и `reference_image_url`, а `product_image_url` задаёт как `None`. Но `omni_kie_client.py` вообще исключает все эти аргументы из отправляемых данных: Comet получает текст и настройки длительности/формата. Оставшиеся в оркестраторе запасные ID также не доходят до API. Поэтому этот путь не обеспечивает привязку к нашему аватару и изображению продукта.

   Доказательства: [useSettingsState.ts:790](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/components/screens/Settings/useSettingsState.ts:790>), [manual-run/route.ts:90](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/app/api/automation/final-videos/manual-run/route.ts:90>), [final_video_automation.py:246](</Users/nadaraya/Desktop/OMNI REELS ANTON/services/v1/automation/final_video_automation.py:246>), [final_video_automation.py:415](</Users/nadaraya/Desktop/OMNI REELS ANTON/services/v1/automation/final_video_automation.py:415>), [omni_kie_client.py:167](</Users/nadaraya/Desktop/OMNI REELS ANTON/services/v1/providers/omni_kie_client.py:167>).

   Что изменить: направить ручной и автоматический запуск в одну текущую Omni-очередь, с выбранным провайдером и сохранёнными входными данными. До удаления старых workers проверить незавершённые задания и перенести необходимые настройки. Наличие старых сервисов в Compose подтверждено, их фактическая активность на сервере не проверена.

3. **P1. Ошибка анализа выбранного референса разрешает взять другой. Подтверждено существующим тестом.**

   Только первая попытка передаёт выбранный `legacyScenarioId`. После неудачи следующая получает `null`, и поиск переключается на ротацию активных источников. Предел — 16 попыток. Замена попадает в данные и журнал, но код не сохраняет обязательство обработать именно выбранный пользователем оригинал.

   Доказательства: [generated-script-reference-selection.ts:39](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/generated-script-reference-selection.ts:39>), [generated-script-reference-selection.ts:102](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/generated-script-reference-selection.ts:102>), [test_omni_reference_skip_contract.mjs:67](</Users/nadaraya/Desktop/OMNI REELS ANTON/scripts/test_omni_reference_skip_contract.mjs:67>).

   Что изменить: выбранный референс фиксировать на весь запуск; повторять его анализ или показывать ошибку этого источника. Ротацию сохранить только для режима, в котором пользователь поручил системе самостоятельно выбрать референс.

4. **P1. Неполный анализ автоматически ослабляет перенос до «общего стиля». Подтверждено исходниками.**

   `hasCompleteSourceTimeline` проверяет наличие визуальных, сценических и камерных данных для всех интервалов. Если проверка не пройдена, выбирается `style_only`: камера и окружение адаптируются, опорный реквизит убирается, а промпт режиссёра разрешает заново поставить сцены и локации. То есть нехватка информации превращается в изменение творческой задачи.

   Даже при полном анализе остаётся другой конфликт: строгий перенос требует сохранять исходные роли и действия, а правила продукта правильно запрещают взаимодействие с аватаром. Нужна явная адаптация конкретного интервала в товарный B-roll до выпуска всех промптов. Сам запрет взаимодействия сохраняется.

   Доказательства: [omni-reference-transfer-policy.ts:58](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-reference-transfer-policy.ts:58>), [omni-reference-transfer-policy.ts:356](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-reference-transfer-policy.ts:356>), [llm-prompt-chain-prompts.ts:132](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/llm-prompt-chain-prompts.ts:132>), [llm-prompt-chain-prompts.ts:178](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/llm-prompt-chain-prompts.ts:178>).

   Что изменить: для основного режима требовать достаточный анализ референса; недостающие факты доанализировать или остановить подготовку. Сохранять сеттинг и сценарную механику, но заранее отмечать требуемые изменения: наш разговорный аватар и отдельные товарные B-roll. После адаптации промпты должны следовать этому плану. Если свободная адаптация нужна как отдельная функция, выбирать её явно.

5. **P2. Предпросмотр выполняет генеративную работу, а создание ролика повторяет подготовку. Подтверждено; повторная оплата изображений зависит от изменения плана.**

   `GET /generated-scripts/[scriptId]/prompts` вызывает построение плана, физическое исправление, семантическую проверку/исправление и запускает генерацию картинок в фоне. При создании ролика `createOmniReel` снова строит и проверяет план. Семантическая проверка вызывается на обоих путях; в каждом вызове допускается до семи LLM-запросов с учётом исправлений.

   Картинки имеют кэш и блокировку, поэтому утверждать, что они всегда генерируются дважды, нельзя. Однако кэш зависит от полного содержания плана: новая правка действия или камеры меняет подпись. Итоговый план может отличаться от показанного ранее и потребовать новых картинок.

   Доказательства: [prompts/route.ts:6](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/app/api/omni/generated-scripts/[scriptId]/prompts/route.ts:6>), [generated-scripts.ts:180](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/generated-scripts.ts:180>), [generated-scripts.ts:228](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/generated-scripts.ts:228>), [reels.ts:265](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/reels.ts:265>), [storyboard-cache-signature.ts:14](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard-cache-signature.ts:14>), [storyboard-semantic-repair-state.ts:3](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard-semantic-repair-state.ts:3>).

   Что изменить: подготовить и сохранить одну версию сценария, плана, изображений и входных данных. Просмотр читает эту версию; генерация использует её. Изменение аватара, продукта, сценария или референса создаёт новую версию по явному действию. Сохранить локальную валидацию, кэш, блокировки и возобновление provider tasks.

6. **P2. Настройки обслуживают уже отключённые функции. Подтверждено исходниками.**

   Экран рендерит только `AutomationSettings`, но его хук занимает 869 строк и содержит состояние HeyGen, голосов, загрузки продукта и оптимизации промптов. Открытие настроек включает запросы к четырём отсутствующим в текущем приложении маршрутам: `/heygen/avatars`, `/heygen/catalog`, `/minimax/voices`, `/elevenlabs/voices`. В текущем `next.config.ts` нет перенаправлений этих запросов.

   Доказательства: [Settings/index.tsx:42](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/components/screens/Settings/index.tsx:42>), [page.tsx:215](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/app/page.tsx:215>), [useWorkspaceData.ts:149](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/hooks/useWorkspaceData.ts:149>).

   Что изменить: оставить состояние и запросы реально доступных настроек. Перенос управления автоматикой из пункта 2 делать в этом же месте. Удаление старого редактора субтитров не означает удаления действующего рендера субтитров.

7. **P1. Видеопромпт не имеет единого явного таймлайна адаптированных кадров. Подтверждено исходниками и локальным вызовом рендереров.**

   Компактный рендерер задаёт число эпизодов и полную реплику один раз, но не выводит временные интервалы, действия и режим речи каждого адаптированного кадра. Изображение раскадровки несёт визуальную информацию, однако само по себе не задаёт однозначно, где включить lip-sync, а где продолжить закадровую речь.

   При физическом/семантическом исправлении в режиме `full_reference` к этому тексту может добавляться `renderReferenceSegmentPlanForPrompt`. Он выводит интервалы исходного референса вместе с исходными `shown`, `speech` и `subject`. В воспроизведении его вывод сохраняет «исходный ведущий держит исходный товар», хотя адаптированный товарный кадр уже требует «без людей и рук». Кроме того, текст одновременно содержит локальное время сегмента и время исходного видео.

   Доказательства: [omni-storyboard-renderer.ts:50](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard/omni-storyboard-renderer.ts:50>), [omni-storyboard-renderer.ts:122](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard/omni-storyboard-renderer.ts:122>), [omni-physical-repair-pipeline.ts:152](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-physical-repair-pipeline.ts:152>), [reference-segment-plan.ts:349](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/reference-segment-plan.ts:349>).

   Что изменить: выводить в Omni один таймлайн утверждённого адаптированного плана, со временем от нуля внутри конкретного видеосегмента. Для каждого интервала указать панель/кадр, аватар или B-roll, действие, камеру, физическое состояние и `on_camera`/`voiceover_only`. Исходные таймкоды оставить для анализа и сопоставления. Полную точную реплику передавать один раз; при переходе на B-roll голос продолжает её без перезапуска.

8. **Отложено по бюджету: проверка физики готового видео. Не входит в текущий план реализации.**

   `storeCompletedSegment` скачивает видео, сохраняет его, извлекает кадр для продолжения и записывает `completed`. Пригодность последнего кадра не доказывает правильность физики на протяжении видео. Проверка изображений раскадровки также не подтверждает отсутствие деформации или левитации после их анимации.

   Доказательства: [omni-segment-completion.ts:15](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-segment-completion.ts:15>), [omni-frame-continuity.ts:87](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-frame-continuity.ts:87>).

   Решение пользователя: сейчас не добавлять платную проверку результата. Использовать ограничения постановки и проверки до генерации. Этот пункт фиксирует предел текущей проверки, а не блокирует запуск проекта и не требует нового сервиса. Вернуться к проверке готового видео можно при отдельном решении о бюджете.

9. **P1. Скорость озвучки оценивается числом слов; длина и произносимость фразы не учитываются. Подтверждено исходниками.**

   Сейчас панель длится две секунды, целевое число слов — четыре, допустимый диапазон — три–четыре. Прежнего диапазона три–пять в этом общем модуле уже нет. При 17 словах и десяти секундах планировщик выдаёт `4 / 4 / 3 / 3 / 3`, что подтверждено локальным вызовом. Недогрузка трёхсловных блоков не получает штрафа за низкую плотность: отношение 0,75 выше текущего порога 0,72. Это не доказывает причину конкретного сбоя модели, но показывает пробел оценки.

   «Вот наш товар здесь» и «Рассматриваем особенности представленной упаковки» содержат по четыре слова, но соответственно пять и восемнадцать русских гласных. Счёт гласных — дешёвая приблизительная оценка слоговой нагрузки, не измерение длительности произнесения. Текущий планировщик считает обе фразы одинаковыми по вместимости. Разделитель блоков лишь помечает конец предложения/продолжение, но не учитывает эту нагрузку при распределении.

   Доказательства: [omni-storyboard-timing.ts:4](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/omni/storyboard/omni-storyboard-timing.ts:4>), [omni-storyboard-timing.ts:42](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/omni/storyboard/omni-storyboard-timing.ts:42>), [omni-duration-planner.ts:245](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-duration-planner.ts:245>), [omni-storyboard-speech.ts:20](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard/omni-storyboard-speech.ts:20>).

   Предлагаемое правило: сохранить четыре слова на двухсекундный блок как базовый ориентир пользователя; не возвращать диапазон три–пять. При написании сценария выбирать короткие разговорные формулировки без скороговорок, цепочек длинных слов и ненужных пауз. Точное название и факты продукта сохранять. Не дополнять недогруженные блоки пустыми словами автоматически; выявлять их до утверждения сценария и корректировать формулировку в существующей стадии работы со сценарием.

   Локально проверять число слов, примерную слоговую нагрузку, пунктуацию и расположение длинных названий. Численный порог слогов не объявлять доказанным оптимумом: калибровать по уже доступным удачным роликам/таймингам, если они есть, без новой платной транскрибации. До калибровки использовать это как диагностический сигнал, а не автоматическое изменение длительности или текста.

   Видеопромпт должен задавать ровный разговорный темп, чёткие окончания и одну непрерывную реплику на весь сегмент. Четырёхсловный блок нужен для сопоставления речи с изображением; его граница не должна заставлять диктора останавливаться и заново начинать речь. На B-roll голос продолжает ту же фразу. Короткие естественные паузы допустимы, добавленные слова/междометия и повторы запрещены. Длинное название нельзя компенсировать ускорением до потери разборчивости.

   Запрет добавлений уже есть в [omni-storyboard-renderer.ts:122](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/storyboard/omni-storyboard-renderer.ts:122>); простое повторение этого запрета проблему не решает. Приоритет — качество исходной фразы, заполнение речевого бюджета и отсутствие конфликтующих требований к паузам/таймингу. Дополнительную LLM-цепочку для этого не создавать.

Проверка документации о посекундном промпте выполнена 4 сентября 2026 года. Google официально поддерживает описание событий во времени и интервалы вида `[0-3s]`, `[3-6s]`, `[6-10s]`; специальный синтаксис не обязателен, допустим обычный язык. Требование описывать каждую секунду и доказательство превосходства такого шага в документации не приведены. Для этого проекта рекомендую интервалы по реальным кадрам и монтажным событиям утверждённого плана. Это инженерная рекомендация, не результат сравнения платных генераций. [Google: Timing events](https://ai.google.dev/gemini-api/docs/omni#timing-events).

Документация Google описывает `gemini-omni-1.1-flash`, а текущий адаптер проекта по умолчанию отправляет в KIE `gemini-omni-video`. KIE принимает текстовый `prompt` и отдельную `duration`; требования к специальному посекундному полю нет. Совпадение версий модели и точность соблюдения таймкодов через посредника не проверены реальным запросом. Менять транспорт изображений, идентификаторов и теги ссылок на медиа по документации другого API автоматически нельзя. [KIE: Gemini Omni Video](https://docs.kie.ai/market/gemini-omni-video).

Разделение двух промптов должно быть таким:

| Раскадровка | Omni-видео |
| --- | --- |
| Как выглядит каждый утверждённый кадр | Когда и как этот кадр оживает |
| Идентичность аватара, упаковка товара, сеттинг, свет, композиция | Интервал времени, движение камеры/объекта, тип монтажного перехода |
| Поза и видимые предметы, опора, масштаб, контактные тени | Непрерывность объекта внутри плана, разрешённая смена сцены на склейке |
| Товар только в назначенных B-roll-панелях, без взаимодействия с аватаром | Аватар говорит в своих кадрах; на B-roll та же реплика продолжается за кадром |
| Без текста реплик и служебных подписей на изображении | Полная точная реплика один раз; изображение раскадровки не показывается как коллаж |

Нынешние «2 секунды на панель» в [omni-storyboard-image-prompt.ts:212](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-storyboard-image-prompt.ts:212>) — правило проекта, а не обязательное требование Omni. Панель раскадровки не обязательно означает новую склейку: две панели могут быть состояниями одного непрерывного плана. Монтажный ритм следует выбирать из референса с учётом нового сценария.

Подтверждённые кандидаты на удаление ниже отсортированы по объёму. Внешних ссылок на эти компоненты/модули в проверенном коде не найдено; внутренние ссылки внутри удаляемой группы учтены. После удаления всё равно нужны сборка и проверка запуска.

| Действие | Группа | Файлов | Строк | Замена |
| --- | --- | ---: | ---: | --- |
| delete | Отключённые компоненты Settings | 10 | 2 326 | Ничего; текущий экран их не использует |
| delete | GraphScreen, LibraryScreen, useAppData | 3 | 1 040 | Существующие активные экраны и hooks |
| yagni/delete | `services/v1/reels_factory/*.py` — заготовка будущей фабрики | 6 | 324 | Ничего; рабочий конвейер уже есть |
| delete | Незарегистрированный Flask Blueprint `routes/v1/automation/content_gen.py` | 1 | 20 | Ничего; активные API находятся в Next |
| **Итого** | **Кандидаты без действующих потребителей в репозитории** | **20** | **3 710** | |

Десять файлов Settings: `BrandingSettings.tsx`, `DeepgramKeywordSettings.tsx`, `HeygenSettings.tsx`, `HeygenAvatarItem.tsx`, `HeygenLookItem.tsx`, `PromptEvolutionSettings.tsx`, `PronunciationSettings.tsx`, `SilenceHandlingSettings.tsx`, `SubtitleSettings.tsx`, `VideoLogicSettings.tsx`. Все находятся в [Settings/components](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/components/screens/Settings/components>).

Остальные группы: [GraphScreen.tsx](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/components/screens/GraphScreen.tsx>), [LibraryScreen.tsx](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/components/screens/LibraryScreen.tsx>), [useAppData.ts](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/hooks/useAppData.ts>), [описание заготовки reels_factory](</Users/nadaraya/Desktop/OMNI REELS ANTON/services/v1/reels_factory/README.md:3>), [Flask Blueprint](</Users/nadaraya/Desktop/OMNI REELS ANTON/routes/v1/automation/content_gen.py:1>).

После удаления графа можно убрать семь прямых JS-зависимостей: `@types/d3-force`, `d3-force`, `react-force-graph-2d`, `react-force-graph-3d`, `reactflow`, `three`, `@types/three`. ReactFlow и D3 импортирует только отключённый граф; использования остальных в исходниках не найдено. Из Python-кандидатов — `flask` после удаления Blueprint и неиспользуемый `python-telegram-bot`; рабочий бот использует `pyTelegramBotAPI`. Это девять записей прямых зависимостей, не оценка числа транзитивных пакетов. `shadcn` оставляем: его CSS импортируется приложением.

Дополнительные сокращения требуют миграции или отдельного подтверждения использования:

- `ScenariosScreen.tsx` — 1 912 строк; старые Generator/Scenarios ветки остаются в `page.tsx`, хотя отсутствуют в текущей навигации. Сначала убрать связанные состояния, callbacks и альтернативные точки входа.
- Старые final-video workers, HeyGen/TTS/montage-ветки и их таблицы: сначала перенести нужные настройки и разобраться с очередями. Они не включены в 3 710 строк.
- `/api/analytics/graph`: 74 строки, единственный найденный UI-потребитель отключён. Внешних потребителей API аудит не проверял.
- [Dockerfile:23](</Users/nadaraya/Desktop/OMNI REELS ANTON/Dockerfile:23>) сохраняет devDependencies и полный исходный репозиторий в финальном образе. После сокращения runtime стоит отделить стадию сборки и убрать ненужные сервисы. Размер экономии не измерялся.
- [.dockerignore](</Users/nadaraya/Desktop/OMNI REELS ANTON/.dockerignore>) не исключает `tmp/`; экспериментальные PNG/JSON могут попадать в `COPY . .`. Проверить нужные runtime-ресурсы и исключить временные данные из сборочного контекста. Пользовательские эксперименты в этом аудите не удалялись.

Что необходимо сохранить:

- Анализ видео и расшифровку референса, проверенный таймлайн, композицию/PiP, одежду, камеру и монтажный ритм. При конфликте исходного действия с требуемым форматом явно адаптировать его в отдельный B-roll.
- Адаптацию сценарной структуры под реальные свойства продукта и CTA; проверки смысла, чисел, речи и порядка слов.
- Сохранённый разговорный аватар и его provider identity, изображения продукта, визуальное описание упаковки и физические ограничения. Сохранить [правило товарного B-roll](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-product-broll-contract.ts:1>): без прямого взаимодействия аватара с товаром.
- Проверки плана и раскадровки до генерации, включая физику и точную речь. Несколько видов проверки не являются мусором только потому, что их много; сокращать нужно повторную работу и противоречащие друг другу исправления. Платную проверку готового видео сейчас не добавлять.
- Provider task IDs, блокировки, кэш, лимиты повторов, сохранение состояния и возобновление задач; FFmpeg и хранилище.
- Telegram ingestion и пока ещё нужные связи с legacy-данными. Длительности читаются из клиентских настроек в [omni-duration-settings.ts:20](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-duration-settings.ts:20>), субтитры — из `clients` в [omni-project-subtitle-settings.ts:35](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-project-subtitle-settings.ts:35>), папка Яндекс.Диска — в [omni-video-storage.ts:106](</Users/nadaraya/Desktop/OMNI REELS ANTON/ui/src/lib/server/omni/omni-video-storage.ts:106>). Удалять всё с названием `legacy` нельзя.

Предлагаемый рабочий процесс: выбранный референс → анализ визуала и сценарной структуры → адаптация под разговорный аватар, тематические и товарные B-roll → локальная проверка речевого бюджета → единый сохранённый план → раскадровка и существующая проверка → временной промпт Omni из этого же плана и локальная проверка согласованности → генерация и сборка ролика. Новая платная проверка готового видео отсутствует.

Порядок работ:

1. Сохранить товарный B-roll без взаимодействия с аватаром. Согласовать физические правила с существующим визуальным QA раскадровки и локальным preflight. Проверку фактического видео отложить по бюджету.
2. Подключить ручной и автоматический запуск к одной Omni-очереди. Отсутствие нужного аватара или product reference должно быть понятной ошибкой подготовки.
3. Проверять заполнение и произносимость речевых блоков с ориентиром четыре слова. Зафиксировать выбранный референс и одну адаптированную версию плана для предпросмотра, раскадровки и временного промпта Omni. Убрать конфликт исходных действий с товарным B-roll, неявную свободную постановку и повторную генеративную подготовку при чтении.
4. Удалить подтверждённые неиспользуемые файлы и зависимости, сократить настройки. Перенести нужные legacy-настройки, затем отключить лишние workers.
5. Проверить подготовку на сохранённых данных трёх референсов: говорящий человек; человек с продуктом в руках, адаптированный в отдельный товарный B-roll; монтаж/PiP с разговорным аватаром. Сравнить планы и промпты, физические ограничения, речевой бюджет и отсутствие повторной генерации при просмотре. Эти проверки не должны требовать новых платных видео или их платного анализа.

Результаты локальной проверки:

| Проверка | Результат |
| --- | --- |
| `node scripts/test_omni_reference_skip_contract.mjs` | Прошла; подтверждает существующее переключение выбранного источника на запасной |
| `node scripts/test_omni_director_visual_boundary.mjs` | Прошла; сетевые ответы подменены тестом |
| `node scripts/test_omni_storyboard_cache_signature.mjs` | Прошла; изменение действия меняет подпись плана |
| `node scripts/test_omni_reference_segment_plan.mjs` | Ошибка на строке 89: ожидалось 3 reference frames, получено 2 |
| Локальный вызов `normalizePhysicalStoryboardSegment` с сохранением действия референса | Подтверждена замена кадра «аватар держит продукт» на предметный B-roll; после уточнения пользователя это требуемое поведение |
| Локальный вызов `normalizeStoryboardVisionValidation` с искусственными физическими ошибками | Три ответа с ошибками получили `pass`; подтверждено ослабление/удаление этих нарушений |
| Локальные вызовы компактного видеорендерера и рендерера source timeline | Первый не выводит временной план адаптированных кадров, второй сохраняет исходное взаимодействие с товаром |
| Локальная проверка речевого бюджета | 17 слов на 10 секунд дают `4 / 4 / 3 / 3 / 3`; две четырёхсловные фразы имеют разную слоговую нагрузку |

Последняя ошибка теста означает расхождение ожиданий и текущей реализации. Без дополнительного разбора требований её нельзя объявлять доказательством неверного числа кадров в продакшене. Полная сборка не запускалась, поскольку код приложения не менялся. При очистке её нужно выполнить вместе с целевыми проверками.

CodeGraph оказался неполным: для проекта вернул один индексированный файл; навигация и подтверждение связей выполнены по исходникам. Независимый Codex-проход проверил legacy/UI-часть, его ключевые выводы перепроверены. Google subagent не выполнил аудит: Antigravity в headless-режиме автоматически отклонил `read_file`; его заключение в отчёте не используется. Указанный в AGENTS проектный UI-skill отсутствует по заданному пути; визуальный аудит работающего интерфейса и редизайн в эту проверку не входили.

net: -3 710 строк, -9 прямых зависимостей возможно. Это проверенный список кандидатов, а не уже выполненное удаление; крупные действующие legacy-ветки в оценку не включены.
